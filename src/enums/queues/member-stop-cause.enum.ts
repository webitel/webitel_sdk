enum MemberStopCause {
  MISSED = 'missed',
  TIMEOUT = 'timeout',
  FAILED = 'failed',
  ABANDONED = 'abandoned',
  SUCCESS = 'success',
  CANCEL = 'cancel',
  EXPIRED = 'expired',
}

export default MemberStopCause
