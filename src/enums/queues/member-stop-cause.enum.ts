enum MemberStopCause {
  MISSED = 'missed',
  TIMEOUT = 'timeout',
  FAILED = 'failed',
  ABANDONED = 'abandoned',
  SUCCESS = 'success',
  CANCEL = 'cancel',
  EXPIRED = 'expired',
  CANCELED_BY_TIMEOUT = 'canceled_by_timeout'
}

export default MemberStopCause
